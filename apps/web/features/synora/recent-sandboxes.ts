/**
 * Проекты сайдбара «Синоры» — аналог PROJECT_GROUPS у Genora
 * (lib/recent-chats.ts), но со своим контентом площадки.
 *
 * ЕДИНЫЙ ИСТОЧНИК ДАННЫХ о проектах и их ветках: каталог
 * picker'а composer'а (composer/projects/data.ts) и реестр веток
 * (composer/branches/data.ts) строятся из этого списка. Новый
 * проект достаточно добавить сюда — он появится в сайдбаре,
 * в списке проектов composer'а и в поповере веток.
 *
 * Работа идёт через GitHub без локального репозитория, поэтому
 * каждый проект — ветка репозитория (иконка ветки в сайдбаре),
 * а внутри — чаты, привязанные к этой ветке. Ссылка ?project=
 * из сайдбара и выбор в picker'е ведут к одному контексту.
 */

export type ProjectGroup = {
  /* Название проекта — совпадает с label в каталоге проектов. */
  name: string;
  /* Ветка GitHub, в которой ведётся работа над проектом. */
  branch: string;
  /* Чаты, ведущиеся в контексте этого проекта. */
  chats: string[];
};

/* Реалистичный кейс: работа над существующим монорепозиторием
   Genora (apps/web — Genora и Synora, packages — общий UI-кит).
   Проект = часть монорепы со своей рабочей веткой, чаты — реальные
   задачи, которые в этих ветках велись. */
export const SYNORA_PROJECT_GROUPS: ProjectGroup[] = [
  {
    name: "Genora — веб-клиент",
    branch: "v0/mobile-profile-layout",
    chats: [
      "Шторка профиля на мобильных",
      "Единый радиус модальных окон",
      "Усечение email в строке профиля",
    ],
  },
  {
    name: "Synora — песочницы",
    branch: "feat/composer-projects",
    chats: [
      "Синхронизация проекта и ветки",
      "Picker проектов в композере",
    ],
  },
  {
    name: "UI-кит @genora/ui",
    branch: "feat/design-tokens",
    chats: ["Токены радиусов и теней"],
  },
];
